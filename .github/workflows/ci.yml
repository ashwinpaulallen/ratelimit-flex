name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Fails the job if the advisory database reports unresolved issues at or
      # above moderate (low is often noisy or disputed).
      - name: npm audit
        run: npm audit --audit-level=moderate

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Vitest 4+ requires Node >=20.12 (uses util.styleText via rolldown)
        node-version: [20, 22, 24]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Test
        env:
          PG_STORE_TEST: '1'
          MONGO_STORE_TEST: '1'
          DYNAMO_STORE_TEST: '1'
        run: npm test

      - name: Test with coverage
        env:
          PG_STORE_TEST: '1'
          MONGO_STORE_TEST: '1'
          DYNAMO_STORE_TEST: '1'
        run: npm run test:coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-node-${{ matrix.node-version }}
          path: coverage
          if-no-files-found: warn
